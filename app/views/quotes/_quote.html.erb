<div class="quote">
  <%= link_to quote.name, quote_path(id: quote.id) %>
    <div class="quote_actions">
      <%= button_to "Delete", quote_path(quote), method: :delete, class:"btn btn--light" %>
      <%= link_to "Edit", edit_quote_path(id: quote.id), class: "btn btn--light"%>
    </div>
</div>